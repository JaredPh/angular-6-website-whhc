import { NgModule } from '@angular/core';
import { ClubsService } from './clubs.service';

@NgModule({
  imports: [
  ],
  declarations: [
  ],
  providers: [
    ClubsService,
  ],
  exports: [
  ],
})
export class ClubsModule { }
